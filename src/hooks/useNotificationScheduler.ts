import { useEffect } from 'react';

// 이 훅은 이제 서버 푸시 알림으로 대체되었으므로 모든 로직을 제거합니다.
// 훅 자체는 앱의 다른 부분에서 호출될 수 있으므로 파일은 유지합니다.
export const useNotificationScheduler = () => {
  useEffect(() => {
    // 아무것도 하지 않음
  }, []);
}; 